
function theBeatlesPlay(musician, instrument) {
  var Beatles = {};

  for (var = i; i <= Beatles.length; i++) {
    Beatles.push(musician[i] + "plays " + instrument[i]);
    console.log(Beatles[i]);
  }
}
